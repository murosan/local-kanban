version: '2'

formatters:
  enable:
    - gofmt
    - gofumpt
    - goimports
    - golines
  settings:
    gofmt:
      rewrite-rules: { pattern: 'interface{}', replacement: 'any' }
    goimports:
      local-prefixes: [localkanban]

run:
  timeout: 5m
